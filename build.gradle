
version = '1.0.0'

apply plugin: 'java'

repositories {
	flatDir {
		name 'local_repo'
		dirs 'C:/Users/broadbear/local_repo'
	}
	mavenCentral();
}

dependencies {
	compile 'org.mike:graph:1.0.0'
	testCompile 'junit:junit:4.12'
}

task wrapper(type: Wrapper) {
//	gradleVersion = '2.0'
}

jar {
	manifest {
		attributes("Implementation-Title": "Gradle",
					"Implementation-Version": version)
	}
}

uploadArchives {
	repositories {
		add project.repositories.local_repo
	}
}